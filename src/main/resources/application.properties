# profile
# spring.profiles.active = 값을 적지 않는 경우 default 사용
spring.profiles.active=st

# database
# spring.datasource.url ? spring.datasource.jdbc-url? ????? ??.
spring.datasource.url = jdbc:mysql://localhost:3306/board?autoReconnect=true&allowMultiQueries=true&characterEncoding=UTF-8&useTimezone=true&serverTimezone=Asia/Seoul&zeroDateTimeBehavior=convertToNull
spring.datasource.username = umoo
spring.datasource.password = q1w2e3r4
#spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver

# server port 변경하는 방법
# server.port=80

#jpa 로그 보기
spring.jpa.show-sql=true
#jpa 로그 보기 좋게 보기
spring.jpa.properties.hibernate.format_sql=true
# datasource 초기화 방법이 변경됨으로 인한 처리
spring.jpa.defer-datasource-initialization=true

#jpa ? 부분 보이기
logging.level.org.hibernate.type.descriptor.sql=TRACE

# 외부 css 연결을 위해 정적 파일 폴더 설정 (안되는 듯?)
spring.mvc.static-path-pattern=/static/**

# thymeleaf 캐시 없애기 (html 수정만으로 변경사항 확인 가능)
spring.thymeleaf.cache=false

# dev tool on
spring.devtools.restart.enabled=true

# 저장 장소 설정
custom.path.upload-files=/files/**
custom.path.upload-images=/files/images/**

custom.path.images=C:/Umoo/study/src/main/resources/static/files/images/
custom.path.files=C:/Umoo/study/src/main/resources/static/files/
